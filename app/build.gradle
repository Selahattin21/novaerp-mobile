plugins {
    id 'com.android.application'
}

android {
    namespace "com.novaerp.mobile"
    compileSdk 34

    defaultConfig {
        applicationId "com.novaerp.mobile"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            shrinkResources false
        }
    }
}

dependencies {
}
